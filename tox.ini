[tox]
envlist = py39
skipsdist = True

[testenv]
commands =
    pip install -r requirements.txt
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = True
source = my_project/
branch = True